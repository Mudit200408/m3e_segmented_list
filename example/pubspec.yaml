name: m3e_segmented_list_example
description: "Demonstrates how to use the m3e_segmented_list package."
publish_to: 'none'
version: 0.0.1

environment:
  sdk: ^3.11.0

dependencies:
  flutter:
    sdk: flutter
  material_ui: ^1.0.0
  m3e_segmented_list:
    path: ../

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
